# Figma assets needed

Figma connector access required reauthentication on 2026-06-27, so no production assets were downloaded. Export these nodes as transparent PNG/WebP (or SVG where vector) and keep the filenames below.

- Package selection node `2:22`: Basic character ? `public/assets/packages/basic-character.png`; Duo character ? `public/assets/packages/duo-character.png`; Premium character ? `public/assets/packages/premium-character.png`.
- Payment node `8:105`: any decorative QR surround ? `public/assets/figma/payment-qr-surround.svg`.
- Preview/camera/result node `28:14`: every actual background image ? `public/assets/figma/background-XX.webp`; frame overlays ? `public/assets/frames/frame-XX.png`; sticker images ? `public/assets/stickers/sticker-XX.png`.
- “SEE U LATER” / Frame 11: central illustration/photo strip ? `public/assets/figma/see-u-later.png`.

The UI uses styled placeholders until these files are supplied. Do not replace them with remote Figma URLs.
